<div id="<%= dom_id post %>">
  <% if user_signed_in? %>
    <p>
      <strong>User:</strong>
      <%= post.user_id %>
    </p>
  <% end %>

  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
